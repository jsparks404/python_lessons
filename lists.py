                # Lists
lucky_numbers = [4, 7, 34, 26, 31, 44, 3, 1]
friends = ['Zach', 'Dave', 'Matt', 'Andrew']

# friends[-1] = 'Andrew'
# friends[1:] = 'Dave', 'Matt', 'Andrew'
# friends[1:3] = 'Dave', 'Matt'
# friends[1] = 'Mike'--> 'Dave' = 'Mike'

        # List methods

    # friends.extends(lucky_numbers) = ['Zach', 'Dave', 'Matt', 'Andrew', 4, 7, 34, 26, 31, 44, 3, 1]
    # friends.append(friend) = adds item to end of list
    # friends.insert(position, "kelly") = adds kelly to index 1
    # friends.remove('Kelli') = removes kelly
    # friends.clear() = []
    # friends.pop() = removes last item from list
    # friends.index('Andrew') = 3    gets error if not in list
    # friends.count('Dave') = 1
    # lucky_numbers.sort() = [1, 3, 4, 7, 26, 31, 34, 44]  sorts in ascending order
    # lucky_numbers.reverse() = reverses order of list
    # friends2 = friends.copy() = copies friends into friends2




                # Tuples
            # Tuples are immutable
coordinates = (3, 5)